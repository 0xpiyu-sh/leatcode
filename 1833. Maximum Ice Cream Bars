class Solution {
public:
    int maxIceCream(vector<int>& costs, int coins) {
        sort(costs.begin(), costs.end());

        int a = 0; 
        int b = 0; 

        for(int i = 0; i < costs.size(); i++) {
            if(a + costs[i] <= coins) {
                a += costs[i];
                b++;
            } else {
                break;
            }
        }

        return b;
    }
};
