class Solution {
public:
    double myPow(double x, int n) {
        double a=0;
        double z=n;
        if(n<0){
            x=1/x;
            z=-z;
        }
        a=pow(x,z);
        return a;
    }
};
