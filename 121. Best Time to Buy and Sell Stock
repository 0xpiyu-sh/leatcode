class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int a = prices[0];
        int pro = 0;
        for (int i = 1; i < prices.size(); i++) {
            pro = max(pro, prices[i] - a);
            a = min(a, prices[i]);
        }
        return pro;
    }
};
