class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        for(int i = 0; i < matrix.size(); i++){
            if(count(matrix[i].begin(), matrix[i].end(), target) > 0){
                return true;
            }
        }
        return false;
    }
};
